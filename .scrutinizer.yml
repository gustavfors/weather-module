
imports:
    - php

filter:
    excluded_paths: [vendor/*, test/*]

build:
    nodes:
        analysis:
            tests:
                override:
                    - php-scrutinizer-run

        tests-and-coverage:

            test2:
                before:
                    command: "echo 'hello world!'"

            tests:
                before:
                    command: "touch .env && echo 'IP_API_KEY' >> .env"
                override:
                    -
                        command: "make phpunit"
                        coverage:
                            file: "build/coverage.clover"
                            format: "php-clover"