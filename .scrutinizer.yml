
imports:
    - php

filter:
    excluded_paths: [vendor/*, test/*]

build:
    nodes:
        analysis:
            tests:
                override:
                    - php-scrutinizer-run

        tests-and-coverage:
            tests:
                before:
                    command: "touch .env && echo IP_API_KEY=123 >> .env && echo WEATHER_APIKEY=123 >> .env"
                override:
                    -
                        command: "cat .env"
                        # coverage:
                        #     file: "build/coverage.clover"
                        #     format: "php-clover"